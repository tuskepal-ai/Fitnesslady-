# Security Baseline

This document outlines the minimal security baseline for documentation files.

- All external links use `rel="noopener noreferrer"` for security.
- All JavaScript examples use strict mode.
- No inline scripts are allowed.
- Sensitive information is never included.

Please review all docs to adhere to these standards.